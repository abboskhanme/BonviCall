# Assumptions

Decisions taken without asking the user, so that work could continue. One line
each: `- [date] <question> → <decision> → <reason>`.

A client corrects a written assumption far faster than they answer a blank
question. Anything here is **in effect** until someone says otherwise.

Revision 2 (2026-09-04): the client answered Q1, Q2, Q3 and Q5. Their answers are
recorded in §"Settled by the client" below. **Six assumption lines were
contradicted and have been removed or rewritten** — they are listed in
§"Withdrawn" so nobody rebuilds against them.

---

## Settled by the client, 2026-09-04

These are decisions, not assumptions. Recorded here so there is one place to look.

- [2026-09-04] SIM fleet or real PBX? → **SIM fleet**: calls ride ordinary mobile SIMs, recorded on the handset, transferred to our server as soon as the internet returns → the client's own words ("telefonida yoziladi, internet yonishi bilan bizni serverga o'tkazishi kerak"); a PBX needs an operator contract and licensing wait, and the CallSentry trial proved the SIM model captures two-sided audio. PBX stays documented as the release-2 fallback.
- [2026-09-04] Who owns the phones and SIMs? → **The handsets are the employees' personal property; SIMs are usually company-issued, sometimes personal** → client's answer. This is the single most consequential fact in the project: it removes managed-device provisioning, makes battery and data budgets an adoption requirement, and means Bonvi cannot wipe, update or replace a device by fiat.
- [2026-09-04] What is the identity anchor — device or number? → **The registered phone number.** Admin enters the agent's number into the panel and capture/attribution happen against it → client's words: "eng muhimi ularning tel raqamini bizning bazaga kiritib qo'ysak shu bo'yicha record qilish". It is also the better key: it survives a phone change and matches BonviZvonki's last-9-digits convention.
- [2026-09-04] Is employee consent required before capture? → **No. Installation is mandatory and there is no consent gate** → client's words: "barchasi o'zimizning xodimlar, ruxsat kerakmas, majburiy o'rnatiladi".
- [2026-09-04] Are the legal opinion, the consent/notice paperwork and SMS in scope? → **No — all three removed entirely, out of scope (not deferred)** → client's words: "loyihadan yurist hulosasi, siyosiy hujjatlar, SMS degan qismlarni olib tashla, kerakmas, bu shunchaki kompaniya ichki loyihasi" — this is an internal company project. Withdrawn in place, ids retained for `docs/TASKS.md`: UC-28, N29, N30, B1.
- [2026-09-04] Where may the audio physically live, now that the jurisdiction analysis is out? → **An ordinary engineering choice — cost, latency, backup convenience** → the constraint that used to sit on N29 is gone; the only remaining rule is N20, that it stays on Bonvi-controlled storage and is never publicly reachable, which is an ownership requirement, not a legal one.
- [2026-09-04] Why 12 months' audio retention, now that it is not a compliance number? → **An operational and storage-cost decision** → 12 months keeps steady-state disk at ≈ 250 GB (N18) and covers a full year-on-year comparison; nothing else drives it.
- [2026-09-04] Was CallSentry trialled, and what happened? → **Yes; two-sided audio worked; no lost calls, no overnight service death, no battery or data complaints, nobody turned it off. The problem was installation and permissions** (developer mode and/or disabling Play Protect) → client's answer. This evidence, not my judgement, now orders REQUIREMENTS §4.2.

## Withdrawn — contradicted by Q2, Q3 or Q5

Listed so that nothing is rebuilt against them. Do not reinstate without asking.

- ~~"Assume ~15 agents on company-owned Android phones."~~ → contradicted by Q2: the handsets are personal property. Replaced below.
- ~~"One active device per agent; a second device sets the first to `replaced`."~~ → reframed: the binding is per **registered number**, not per agent, because a number can be reassigned between people over time.
- ~~"Distribution is a signed APK … or MDM push."~~ → MDM push is not available on employee-owned phones. Replaced below.
- ~~"Local queue holds ≥ 2 GB of audio."~~ → halved to 1 GB; that storage belongs to the employee.
- ~~"The internal-line directory is built from every enrolled device's own number."~~ → built from **registered numbers**, which is cleaner and was the point of Q2's answer.
- ~~"Consent is a server-side precondition for accepting audio."~~ → contradicted by Q3, then removed outright by the 2026-09-04 scope reduction. There is no consent gate and no recorded notice artefact.
- ~~"Number verification falls back to a verification SMS."~~ → SMS removed from the project. The callback route is now the only fallback and is therefore load-bearing.
- ~~"The audio storage location must be a deliberate, documented choice because voice may be biometric data under ZRU-547."~~ → the legal analysis is out of scope. Location is chosen on cost, latency and backup convenience.
- ~~"If a model fails the audio threshold, replace the phone."~~ → Bonvi cannot replace a phone it does not own; the remedy is now a budget decision (buy that person a handset) or accepting that the person is uncovered.

---

## From plan-analyst, 2026-09-04

### Scope

- [2026-09-04] Is AmoCRM (or any CRM) integration in scope? → No, out of release 1 → CallSentry's `AmoCrmApiService` + CRM-selection screen were scaffolding for a different customer; Bonvi's CRM-shaped needs live in BonviZvonki, and every CRM adapter is a second wire format to keep alive.
- [2026-09-04] Is SMS in scope in any form? → **No — removed entirely: no sending, receiving, logging, or SMS gateway dependency** → client's scope reduction. Number verification uses the callback route (agent dials a short server number, inbound caller ID confirms), which needs no third-party gateway.
- [2026-09-04] With SMS gone, how does the app prove it is on the registered number when the SIM's MSISDN is empty? → **The callback route is the only remaining fallback, so it must be verified per OEM *and* operator in M0** → inbound caller-ID presentation is decided by the network, not by us; a single untested operator that withholds CLI would block enrolment for everyone on it.
- [2026-09-04] Migrate existing MoyZvonki call history and audio? → No; BonviZvonki keeps its history, BonviCall starts empty → the user said "completely independent", and MoyZvonki only retains 30 days of audio anyway.
- [2026-09-04] Multi-tenant support? → No, single Bonvi instance → no second customer exists; multi-tenancy taxes every query forever.
- [2026-09-04] Are WhatsApp/Telegram/IMO voice calls captured? → No, cellular calls on registered numbers only → no Android API exposes them to a third-party app; the blind spot is documented in §5.6 and goes to the risk register.
- [2026-09-04] Release-1 sizing target? → Design for ~25 registered numbers and ~15,000 calls/month; must not architecturally block 100 → BonviZvonki's measured volume model (15 agents, 11,700 calls/month, 1,560 h audio/month) plus headroom. **The actual model mix is still unknown and is not assumed** (open item D1).

### Personal-device boundary (all new, all consequences of Q2)

- [2026-09-04] What happens to calls on a second, unregistered SIM in the same phone? → **Not recorded, not uploaded, not counted** — no metadata, no number, no audio → capturing an employee's private calls on their own handset is the fastest way to lose the fleet. Stated in the employee manual so the boundary is visible, not merely promised.
- [2026-09-04] What if the OS cannot tell which subscription a call belonged to? → Treat it as **unregistered** (fail-closed) and count it as `subscription_unknown` in the gap report → the OEM-inconsistent `PHONE_ACCOUNT_ID`/`SUBSCRIPTION_ID` columns mean guessing would risk uploading a private call; missing a work call is the lesser failure.
- [2026-09-04] May the app read the employee's contact book? → Only to resolve a name on-device, and a name is uploaded only for a captured registered-number call → on a personal phone the contact book is the employee's private social graph; the number is the identity anyway (BonviZvonki's L3 lesson).
- [2026-09-04] The OEM recorder records *all* calls including private ones — do we harvest from that shared folder? → Only files whose time window matches a captured registered-number call; everything else is ignored and never uploaded → the folder is shared between work and private calls, so the matching rule is the privacy boundary, not just a correctness detail.
- [2026-09-04] How does an agent know what is being captured? → The app permanently displays the recorded number on its main screen, and the agent can see their own calls → on hardware the employee owns this is the difference between a tool and surveillance, and it is the cheapest adoption lever available (N41).
- [2026-09-04] How is one installation per number enforced? → Server-side: verifying on a new phone binds the number to the new installation, sets the previous to `replaced` after accepting its queued records, and raises an admin notification → two live installations double-count calls; an unexpected re-binding is also what a stolen credential looks like.
- [2026-09-04] How is the agent↔number mapping stored? → **Time-boxed** (`valid_from` / `valid_to`), not a plain column → a company SIM reassigned from agent A to agent B must leave A's historical calls attributed to A. BonviZvonki's single `agents.phone` column cannot express this; do not copy it.
- [2026-09-04] Can Bonvi wipe or reclaim the device when someone leaves? → No; the product revokes the token, stops capture and deletes local audio, and shows `revoked_pending_confirmation` if the phone never reconnects → Bonvi does not own the hardware, and a product that implies a completed wipe would be lying.
- [2026-09-04] Who pays for the mobile data? → Applied default: Wi-Fi-first upload with a 1 GB/month cellular cap, and the cost is stated plainly in the employee manual → this keeps the cost negligible; surfaced to the client as business item B2 because an unexplained data charge on a personal phone is exactly what gets an app uninstalled.

### Installation and permissions (elevated by Q5)

- [2026-09-04] How is the app distributed and updated? → Signed APK from Bonvi's own server with an in-app updater; **not** Google Play and **not** MDM → Play policy prohibits call-recording apps and would reject the permission set; MDM requires device ownership Bonvi does not have.
- [2026-09-04] What happens when an employee does not update? → The server enforces a **minimum supported app version**: an old client is refused with an Uzbek "please update" message, but **its queued records are accepted first** → updates now depend on employee cooperation, so version drift is certain; refusing an old client must never destroy data.
- [2026-09-04] What if a step genuinely cannot be completed unaided (developer options, Play Protect exemption)? → The flow says so plainly, offers an admin-assisted path, and the panel marks the agent `needs_assisted_install` → a silently stalled enrolment looks identical to a working one, which is how a rollout quietly fails.
- [2026-09-04] Which `targetSdk` does the app use? → Left open as a live design decision for `plan-stack`/`plan-architect`, flagged as coupled to the recording route → CallSentry deliberately used `targetSdk 28` for legacy file access to `Recordings/Call`, which is a plausible cause of both the two-sided audio *and* the install friction; the two may be the same decision, and a low `targetSdk` has a deprecation clock.
- [2026-09-04] Is Android Enterprise work profile (BYOD) the answer to install friction? → **Not assumed, not recommended; scheduled as spike S2** → it is the standard route to permitting a corporate app on an employee-owned phone, but a work profile may be isolated from the personal dialer and therefore unable to see the calls at all, which would make it useless here.

### Roles and access

- [2026-09-04] Which role model? → Reuse BonviZvonki's `admin` / `manager` / `sales` / `viewer`, plus a machine `service` token → same company, same people; a second mental model would be a permanent tax.
- [2026-09-04] May a salesperson see their own calls and play their own audio in the panel? → Yes, own only, read-only → transparency is the stated mitigation for the biggest organisational risk (BonviZvonki `PLAN.md` R5) and matters more now that the app runs on their own phone.
- [2026-09-04] What does the `viewer` (sales-room TV) see? → Live board only; numbers masked to last 4 digits; no audio, no personal data → the screen hangs in a room visitors walk through.
- [2026-09-04] May anyone delete an individual call or its recording? → No; `DELETE` on a call returns 405 for every role including `admin`. Only the retention job deletes audio → otherwise the first bad call gets quietly removed and the dataset is worthless.
- [2026-09-04] Who may listen to recordings? → `admin` and `manager` with every playback audited; `sales` their own only; `viewer` never → proposed in Q3 and not disputed by the client.
- [2026-09-04] How is "wrong owner" reported to a `sales` user? → 404, not 403 → 403 confirms the record exists and leaks that another agent talked to that number.

### Data, formats and identity

- [2026-09-04] Wire timestamp format? → ISO-8601 with explicit UTC offset, plus the device's raw epoch millis and timezone in the same payload → ISO is self-describing and debuggable; the raw epoch lets the server compute clock skew instead of trusting the handset.
- [2026-09-04] Which timestamp orders the data? → Server receipt time is authoritative; device time is data, not truth → devices are wrong about time and users change it.
- [2026-09-04] Idempotency key for a call? → A client-generated UUID (`client_call_id`) created at call start, server upserts on it → CallSentry used `localId`, a per-install Room autoincrement that restarts at 1 after reinstall and collides across devices.
- [2026-09-04] Phone-number matching key? → Normalise to E.164, match on the **last 9 digits** → identical to BonviZvonki, where it was already solved; a second convention would guarantee mismatched clients.
- [2026-09-04] How is internal vs external decided? → Automatically from **all registered numbers** plus an admin-editable extra list accepting suffix rules (`*700`); < 6 digits = PBX extension = internal; an empty directory yields `unknown`, never `external` → BonviZvonki's hand-maintained setting starved (10 of 33 employees had a number) and a content-based classifier then mislabelled 82 of 98 calls.
- [2026-09-04] How is capture rate measured after go-live, with no adb on personal phones? → The app self-reports: it sweeps its own call log for the registered subscription every 6 hours and reports that count next to what it uploaded; the panel shows the delta → without this, N1 becomes unverifiable the day the acceptance window ends.

### Audio

- [2026-09-04] Audio format and bitrate? → Mono, 16 kHz, Opus ≤ 24 kbps (~10.8 MB/hour), transcoded on the device **before** upload → CallSentry's 44.1 kHz/128 kbps AAC is ~57.6 MB/hour ≈ 6 GB/agent/month of mobile data — unacceptable on a personal data plan; 16 kHz mono is the ASR-relevant band.
- [2026-09-04] Audio retention period? → 12 months, then automatic deletion; metadata and audit log indefinite → **an operational and storage-cost decision**: it holds steady-state disk at ≈ 200–250 GB and covers a full year-on-year comparison.
- [2026-09-04] Where does audio physically live? → One Bonvi-controlled object store; never a public or unauthenticated URL. **Which hosting, and where, is an ordinary engineering choice — cost, latency, backup convenience** → owning the audio is the entire point of the project (L1); beyond that, nothing constrains the location.
- [2026-09-04] Wi-Fi-only audio upload? → Wi-Fi preferred by default, but audio older than 24 h uploads over cellular anyway, subject to a per-device monthly cellular cap (default 1 GB) → a strict Wi-Fi-only rule means an agent who never sees Wi-Fi silently never delivers audio.
- [2026-09-04] Local queue capacity on a phone Bonvi does not own? → ≥ 30 days / ≥ 2,000 calls / **≥ 1 GB** audio; stop recording new audio and alert when the queue is full or the phone's free space drops below 1 GB; never delete captured audio; never drop metadata → 30 days of metadata is under 2 MB, so losing a call record is never the right trade — but filling an employee's personal storage is a fast route to uninstallation.

### Product surface

- [2026-09-04] Panel UI language? → Uzbek only in release 1; Russian and English later → global working rules make client-facing output Uzbek; a second locale buys nothing before the product is proven.
- [2026-09-04] Is the panel responsive/mobile? → Desktop-first, usable on a tablet, not designed for a phone → managers review calls at a desk; the salesperson's surface is the Android app.
- [2026-09-04] Browser support? → Last two versions of Chrome and Edge → matches existing internal tooling and is where the audio-seek + `Authorization` workaround is already proven.
- [2026-09-04] Deployment shape? → Single self-hosted instance on Bonvi-controlled infrastructure, containerised → same operating model as BonviZvonki, run by the same people.
- [2026-09-04] Create/edit UI pattern? → Modal-only, no inline inputs on the page → the house convention in BonviZvonki, enforced there by `qa-review`.
- [2026-09-04] Error response shape? → `{"error":{"code":"...","message":"..."}}` on every endpoint, message in Uzbek where an end user sees it → BonviZvonki convention; a second envelope means a second frontend error path.
- [2026-09-04] Minimum Android version? → API 26 (Android 8.0), and every OS version present in the fleet must be on the supported list before go-live → same floor CallSentry used; the real constraint is the fleet, not the SDK.
- [2026-09-04] Does BonviCall feed BonviZvonki, and when? -> Yes, but not in release 1: release 1 ships a read-only export (UC-29) and BonviZvonki is wired to it in release 2, after one month of parallel running against MoyZvonki -> Client confirmed the connection is planned ("bonvizvonkini keyinchalik bu projectga ulaymiz"); sequencing it this way keeps release 1 demonstrable on its own while making the later join cheap, and the parallel month prevents losing both call sources at once
- [2026-09-04] Server and panel stack? -> FastAPI + SQLAlchemy + PostgreSQL + React/Vite/TS/Tailwind -> Client chose it; BonviZvonki uses the same stack so RBAC, module layout, error conventions and number normalisation transfer as patterns rather than being rewritten
- [2026-09-04] Android targetSdk — 28 (legacy, proven) or 34 (modern, All-files-access)? -> Build BOTH variants in M0 and decide on measured evidence; default to 34 if it captures two-sided audio on the fleet -> The client did not choose and asked about alternatives instead. 34 is better on every axis except proof (no developer mode, no Play Protect fight, no deprecation clock), and M0 is already placing real calls per model, so adding the second variant costs about one day. Deciding on measurement rather than assumption is worth that day. See docs/S1-RECORDING.md
- [2026-09-04] Project justification — cost saving or capability? -> Audio ownership and 12-month retention, NOT cancelling the MoyZvonki subscription -> Client chose this framing. MoyZvonki is 230 RUB/device/month = ~$37/month for 15 agents, which is at or below BonviCall's own running cost; the cost case only turns positive above ~20 devices. MoyZvonki deletes recordings after 30 days, which is the durable argument
- [2026-09-04] Adopt an existing open-source platform instead of building? -> Build. ViciDial is mature but is a PBX (rejected model); the SIM-fleet category has no mature open-source entrant -> See docs/BUILD-VS-ADOPT.md. Revisit ViciDial only if M0 shows audio is unobtainable on most of the fleet

### From plan-conventions, 2026-09-04 (see `docs/CONVENTIONS.md`)

- [2026-09-04] How many backend layers per module? -> Three files (`models.py` / `service.py` / `schemas.py`) plus an optional pure `rules.py`; routers grouped per entry point outside the module -> BonviZvonki's 4-layer split did not do its job: `src/shared/{application,domain,infrastructure}/` are four empty `__init__.py` files, `users/application` and `auth/application` are empty, and `select(` still appears 11 times in `agents/presentation/router.py`. The ceremony cost is real, the separation was not.
- [2026-09-04] Where does the wire contract live and which direction does it generate? -> `contract/openapi-{device,panel,service}-v1.json`, generated from the Pydantic schemas by `make contract`, committed, and CI fails on `git diff --exit-code contract/`; Kotlin DTOs and panel types are generated from it -> `TASKS.md` T23 asks for one artefact driving both clients; code-first is chosen because FastAPI is code-first and a hand-written OpenAPI would rot. The committed artefact makes any wire change visible in the PR diff.
- [2026-09-04] Test database — shared dev DB with a MARK prefix, or a separate one? -> A separate `bonvicall_test`, migrated by `alembic upgrade head` and truncated per test -> BonviZvonki's shared-DB + sweeper convention caused a real incident (test rows in `client_contacts` showed among real clients in the UI). Cost accepted: a second DB in Compose/CI, tens of seconds per run, and losing the ability to inspect a failed test's leftovers in the panel.
- [2026-09-04] Keep the single ORM model registry? -> Yes, `server/src/core/models.py`, but backed by `test_model_registry_complete` which walks the module `models.py` files and asserts every `Base` subclass is reachable -> BonviZvonki has the same requirement as folklore and it fails at runtime with `NoReferencedTableError`; a test turns it into a CI failure.
- [2026-09-04] Where does the permission registry live? -> `server/src/core/permissions.py`, with permissions as `Perm.*` constants rather than string literals -> BonviZvonki keeps `ROLE_PERMISSIONS` in `modules/users/domain/entities.py` and imports it from `core/deps.py`, so `core` depends on a module; and raw strings mean a typo like `require_permission("call:read")` fails closed and silently forever.
- [2026-09-04] How is the UC-15/N28 privacy boundary enforced so no later module can widen it? -> By type: `PrivacyBoundary.evaluate()` returns a sealed `Decision`, and `OemHarvestStrategy.locate()` takes `Decision.Capture` as its argument, so the OEM file scan is unreachable without passing the boundary; plus a second server-side gate (409 `audio_unmatched`) and three named tests that may not be deleted -> A rule in a document can be widened in one line; a signature cannot.
- [2026-09-04] How is `targetSdk` kept a build-variant decision? -> Two product flavors (`legacy28`, `modern34`), and `Build.VERSION.SDK_INT` may appear only in `core/Capabilities.kt` -> CallSentry hard-codes `targetSdk = 28` with no flavor, and S1 shows both recording paths depend on it; the modern-target build must be buildable and measurable without editing source.
- [2026-09-04] Panel loading/empty/error handling? -> One `shared/ui/QueryBoundary.tsx`; `isLoading` is forbidden inside a page -> BonviZvonki has `isLoading` 67 times across 33 page files with no convention, so every empty state differs slightly.
- [2026-09-04] Comment language in BonviCall source? -> English, unlike BonviZvonki and CallSentry which comment in Uzbek; Uzbek in source is legal only in `core/messages_uz.py`, `uz.json` and `strings.xml` -> N39 and the global working rules; both reference repos sit next to this one and copying a file otherwise brings its comment language along.
- [2026-09-04] One conventions file or two? -> Two: `docs/CONVENTIONS.md` (cross-cutting + server) and `docs/CONVENTIONS-CLIENT.md` (panel + Android). Shared rules — contract, errors, time, phone, idempotency, privacy boundary, permissions, naming, testing, language — stay in `CONVENTIONS.md` only and are pointed at, never copied -> ~99 coding tasks each load a conventions file; a `build-backend` agent should not be reading Kotlin threading rules. Duplicating a shared rule across two files would guarantee it drifts.
- [2026-09-04] `TASKS.md` T23 says the contract is spec-first; `CONVENTIONS.md` §1 says code-first with a CI diff gate. Which wins? -> `CONVENTIONS.md`; T23 predates the decision and is being amended by `plan-manager` -> Confirmed by the coordinator 2026-09-04. Noted inline in §1 so nobody reads the two files and thinks the direction is still open.

---

## From build-backend, Phase 1 foundations (SV-CORE / SV-RBAC / SV-SCHEMA), 2026-09-04

`docs/SPEC.md` §0 is the tie-breaker used throughout: **CONVENTIONS wins on
style and structure, SPEC wins on behaviour.** Where that rule did not settle a
conflict, the decision and its reason are here.

### SPEC vs CONVENTIONS conflicts resolved

- [2026-09-04] Mobile API prefix — SPEC §4 says `/api/mobile/v1`, `CONVENTIONS.md` §4 says `/api/device/v1` → **`/api/device/v1`** → the committed contract artefact is already named `contract/openapi-device-v1.json` (CONVENTIONS §1) and the router package is `api/device/`; one of the three had to give, and picking "device" makes the file name, the package and the path say the same word. The panel (`/api/v1`) and service (`/api/service/v1`) prefixes are SPEC's, unchanged. All three live in `server/src/api/__init__.py`.
- [2026-09-04] Error code names — CONVENTIONS §9 says `number_in_use`, `code_used`, `code_expired`, `audio_unmatched`; SPEC §4 says `number_already_assigned`, `enrolment_code_used`, `enrolment_code_expired`, `audio_not_attributable` → **SPEC's names** → an error code is wire behaviour and clients branch on it, so the API contract decides. One name each, no aliases: two names for one code is how a contract forks. Note that CONVENTIONS §8 names a load-bearing test `test_audio_unmatched_rejected` — the test keeps that name, the code is `audio_not_attributable`.
- [2026-09-04] `PUBLIC_ROUTES` size — SPEC §4.1 rule 5 says "exactly five", SPEC §4.2 then makes `POST /enrolment/redeem` public as well → **six entries**, declared in `core/permissions.py` with the reason inline → the redemption call cannot carry a token by construction. T20's harness asserts against the constant, not against the number five.
- [2026-09-04] Server module shape — SPEC §6/§11.1 says `domain/ application/ infrastructure/ presentation/`, `CONVENTIONS.md` §2/§3 says `models.py / schemas.py / service.py / rules.py` → **CONVENTIONS** → it is explicitly a style rule, and §0 rejects the four-directory shape with evidence from BonviZvonki.
- [2026-09-04] RBAC registry location — SPEC §4.1 says `modules/users/domain/entities.py`, `CONVENTIONS.md` §11 says `core/permissions.py` → **`core/permissions.py`** → CONVENTIONS names the BonviZvonki defect being avoided (core importing a module) and §0 makes structure its call.
- [2026-09-04] Settings table name — SPEC §3.8 says `settings`, `CONVENTIONS.md` §10 says "`app_settings`, never `settings`" → **`app_settings`** → naming is a style rule. The ORM class is `AppSettingModel`.
- [2026-09-04] Release-1 module list — CONVENTIONS §3 names `gaps`, `exports`, `catalog`; SPEC §6 names `reports`, `export`, `callback` → **CONVENTIONS' list** → same reason. Only the modules that own a table exist after Phase 1 (`gaps` and `exports` compute, so creating them now would be the empty-layer defect §15 forbids). `callback`'s tables live in `enrolment`, which is where verification belongs.

### Gaps in the SPEC that had to be filled

- [2026-09-04] `audit_action` enum values — SPEC §3.8 deferred them to "§3.16", a section the document did not contain → **a closed set in `core/enums.py::AuditAction`**; `plan-architect` then wrote §3.16 against the shipped set and added two values, so it is now **37** → the column is a native enum, so the list has to exist. The two additions (`retention_changed`, `command_issued`) earn their place by being separately filterable: retention is the one setting whose change destroys data, and issuing a command is the only action in the product that reaches into hardware the company does not own.
- [2026-09-04] **`001_create_release1_schema.py` may still be edited in place — until the first deploy.** The one-history rule (CONVENTIONS.md §10) says a schema change is always a new revision. That rule starts binding **the moment 001 has run somewhere that matters, which is the first deploy to the procured host (W03)**. Before then 001 is the release-1 baseline, has only ever run on development machines, and a revision reading "add two values to an enum that shipped an hour ago, in a release that has not shipped at all" is permanent noise in a history people read. Editing it requires the full round trip every time — `upgrade head`, `alembic check` clean, `downgrade base`, re-upgrade. **After W03 there is no exception.** The same note is at the top of the migration so nobody has to find this line.
- [2026-09-04] `alert_kind` count — SPEC §3.1 says "24 values", §10.3's table expands to 27 once the grouped rows are split → **27 values** → §3.1 was counting table rows, not enum members.
- [2026-09-04] Which module owns which table (SPEC groups tables by topic, not by module) → `devices` owns the handset and all telemetry; `enrolment` owns codes, attempts, verifications, receivers and callback events; `audio` owns `call_audio`, `audio_upload_sessions` and `storage_usage_daily`; `catalog` owns the line directory, supported models, capture stats and app versions; `auth` owns both token tables → cohesion with the module that will read them in Phase 3.

### Schema decisions

- [2026-09-04] `calls.seq` — SPEC says `BIGSERIAL` → **`GENERATED ALWAYS AS IDENTITY`** → identical monotonic counter, but an explicit insert of `seq` becomes an error instead of a silent hole in somebody's export cursor.
- [2026-09-04] The four `calls` CHECKs — SPEC §3.5 writes five constraints while T147 and the Definition of Done both say "four" → **four constraints carrying all five rules**: the two duration rules are one constraint (`ck_calls_answered_has_duration`) → keeps T147's count and loses no rule.
- [2026-09-04] `device_health`, `app_settings` and `storage_usage_daily` have no `UUIDMixin`, against `CONVENTIONS.md` §10's "always `(Base, UUIDMixin, TimestampMixin)`" → **their natural key is the primary key** (`installation_id`, `key`, `period_date`) → SPEC §3.7/§3.8 defines them that way, and a surrogate id would let two rows claim one installation, one setting or one day. `audit_log` additionally has no `TimestampMixin`: the row is immutable by trigger, so `updated_at` could only ever be a lie, and `at` is `created_at` under SPEC's name.
- [2026-09-04] `app_settings.description_uz` is seeded NULL by the migration, although SPEC §3.8 implies the migration seeds the whole row → **key, value and value_type are seeded; the Uzbek descriptions are the settings module's (T58)** → `CONVENTIONS.md` §14 allows Uzbek text in exactly three files and a migration is not one of them.
- [2026-09-04] The generated last-9 columns and the enum value lists are written out literally in the migration rather than imported from `core/` → **literal** → a migration must keep its meaning after the application's enums move on. The literals were *produced by* `core.phone.phone_key_sql` (§7), and two tests keep them honest: the enum table is compared with `core/enums.py`, and the stored key is compared with Python's `phone_key()` for every vector in `contract/phone-vectors.json`.
- [2026-09-04] Where do the ~30 PostgreSQL enums live, given CONVENTIONS has no module for shared wire vocabulary? → **`core/enums.py`** → `capture_route` is written by Android, stored by `calls`, read by `audio` and exported to BonviZvonki; putting it in one module's `models.py` would force every other module to import that module's models, which §2 forbids.

### Test and tooling decisions

- [2026-09-04] Does `conftest.py` build the schema with `create_all` (§10) or `alembic upgrade head` (§13, T154)? → **`alembic upgrade head`, and `create_all` is used nowhere in the project** → the generated columns, the exclusion constraint and the audit trigger exist only in the migration, so `create_all` would test a different schema from the one that ships.
- [2026-09-04] `conftest.py` lives at `server/`, not `server/tests/` → so the module suites at `src/modules/<name>/tests/` (CONVENTIONS §3) see the same thirteen fixtures → twenty modules must not invent twenty definitions of "a call". `pyproject.toml` `testpaths` was widened to `["tests", "src"]` for the same reason.
- [2026-09-04] `structlog` added to `server/requirements.txt` → CONVENTIONS §9 requires it for logging and N26's redaction test, and it was missing from the dependency list.
- [2026-09-04] Ruff line-length is not enforced on `migrations/versions/*.py` → the release-1 migration is largely machine-produced DDL, and rewrapping a `create_table` call by hand is how a column silently loses a constraint. Every other rule still applies to it.
- [2026-09-04] `gaps` and `exports` own no table but read six tables across four modules — service round-trips, or a documented exception? -> A named, fenced exception in `CONVENTIONS.md` §2.1: a module with no `models.py`, declared in `server/src/core/reads.py`, may read exactly the listed foreign models, read-only, in column projections and aggregates only (`select(Model)` forbidden), returning its own `schemas.py` type. Enforced by `test_cross_module_reads_declared` -> Per-row service calls would make the gap report — the product's own smoke alarm — slow enough that someone later "optimises" it with a raw query, breaking the rule by accident. Projection-only means no foreign ORM object can leak, so the boundary is structural rather than remembered. Single-row lookups (`auth` → `UserService`) are explicitly NOT covered.

### Panel shell decisions (T21)

- [2026-09-04] The panel's access token lives **in memory only**, not in `localStorage` → the refresh token is an HttpOnly cookie scoped to `/api/v1/auth/refresh` (SPEC §4.7), and keeping the access token in `localStorage` would hand any XSS exactly what that cookie was chosen to protect. Cost: one silent `POST /auth/refresh` on page load, performed by `useAuth.restore()`. *(BonviZvonki keeps both tokens in `localStorage`; it has no HttpOnly refresh cookie to protect.)*
- [2026-09-04] A mid-session 401 refreshes **once**, deduplicated across concurrent requests, and replays the original request; only when the refresh fails does the store flip to `anonymous` and the router's `Protected` redirect to `/login` carrying `state.from` → six simultaneous 401s from one page's six queries would otherwise fire six refreshes, and the server treats a replayed refresh token as theft (`refresh_reused`, SPEC §4.7).
- [2026-09-04] No i18n library in the panel; `shared/i18n/uz.json` is a **flat, dotted-key catalogue** with a typed `t()` → CONVENTIONS-CLIENT.md §3 rejects BonviZvonki's three-locale setup, and flat keys make `MessageKey` a literal union, so a typo is a compile error instead of a dotted identifier rendered on screen. `errors.<code>` is likewise typed, so an error code with no Uzbek text fails `tsc`, not only the test.
- [2026-09-04] `panel/src/shared/auth/permissions.ts` is a **hand-written mirror** of `core/permissions.py::Perm` → the permission registry is not part of `contract/` (`contract_export.py` emits the three OpenAPI documents and `error-codes.json` only), so there is nothing to generate from. It is a constant list, not a response type, but it is still a copy; **TODO(T103): export `contract/permissions.json` and generate it**, which is a small addition to the existing generator on both sides.
- [2026-09-04] `panel/src/shared/api/types.pending.ts` holds `SessionUser` and `LoginResponse` by hand → the generation toolchain is wired and working (`make types`, `openapi-typescript`), but `contract/openapi-panel-v1.json` currently describes only `/healthz` and `/readyz` because `server/src/api/panel/` has no routers yet. The shell cannot boot without knowing what `GET /auth/me` returns. One file, one TODO naming T24/T148, field names taken from SPEC §4.7 so the replacement is a rename. **Nothing else in the panel declares a response shape.**
- [2026-09-04] SPEC §5.2 gates `/devices` on `devices:read` alone while `/devices/:installationId` also accepts `devices:read:own`, leaving a `sales` user with a device page and no way to reach it → **decided: `/devices` gate and nav entry both carry `devices:read, devices:read:own`, and `DeviceService.list()` narrows to the caller's own installations.** SPEC failed to apply the project's own rule here: scope is narrowed by the *query*, not by the permission (CONVENTIONS.md §11), exactly as `calls:read:own` already works. A salesperson gets a one-row list — their own phone's health — which is the same shape as their calls page. `build-backend` owns the narrowing.
- [2026-09-04] SPEC §5.2 makes `/` "any authenticated", and a `viewer` holds only `monitor:read`, so every dashboard tile would 403 → **decided: after login — and as the `<Gate>`'s refusal target — a user with none of the dashboard's permissions goes to `/monitor`.** A page whose whole contents 403 is not a page, it is a bug with a route. `/` stays reachable for everybody else. The rule is in `panel/src/shared/auth/landing.ts` and asks "is there one tile this user may see?" against the permission list from `GET /auth/me`; it is not a role map (SPEC §5.1). T85 keeps `DASHBOARD_PERMISSIONS` in step with the tiles it renders.
- [2026-09-04] `/i/{code}` is claimed by both surfaces — SPEC §4.1 lists it as a public **server** route and §5.2 as a public **panel** route → **decided: the server answers it with server-rendered HTML; the panel route and `InstallLandingPage` are deleted.** It is the first thing a non-technical salesperson touches, on their own phone, over mobile data, inside N40's 15 unaided minutes and against R17, the top practical risk. A React SPA there costs a bundle download before the first word appears and fails to a blank screen; a few KB of HTML works without JS and serves the APK directly instead of proxying it through a client. T98 builds it on the server. The dev Vite proxy does not forward `/i`, so nothing in the panel shadows it.

### From build-backend, Phase 2 (M1) and Phase 3 Wave A, 2026-09-05

**Layering.** The §2.1 rule landed while this work was in flight and my services
violated the base rule it fences. The fix was mechanical and is worth recording
because it changed where several things live:

- [2026-09-05] Cross-module model imports are now **FK targets only**, checked by `tests/test_layering.py` against the actual foreign keys in `Base.metadata` rather than against anybody's memory → so the rule widens by itself when a column is added and a stale import fails when one is removed. Five reads moved behind services: `calls` asks `NumberService.all_phone_keys()`/`holder_at()` and `CatalogService.directory_rules()`; `agents` asks `NumberService.count_open_assignments()`; `users` asks `AuthService.revoke_all_sessions()`; `enrolment` asks `DeviceService.upsert_from_report()` and `InstallationService.create_pending()`; `installations` asks `DeviceService.queue_snapshot()`.
- [2026-09-05] **Installation-bound tokens moved from `auth` to `installations`** — minting, rotation and the N24 binding check now live in the module that owns `InstallationModel`, and `auth.resolve_principal` dispatches to it → `auth` was importing `installations.models` for no reason except a type annotation, and the binding rule belongs with the row it binds.
- [2026-09-05] `core/reads.py` created with §2.1's declaration, and `tests/test_layering.py` written to enforce all of it (holders own no table, read only their listed models, never write, never `select(Model)`) → the rule constrains code that exists **now**, so the test that proves it should exist now too. `gaps` and `exports` are not built; the test skips a declared module that has no directory yet.

**Security and dependencies.**

- [2026-09-05] `passlib[bcrypt]` replaced by `argon2-cffi` → SPEC §4.7 specifies argon2id, which passlib's bcrypt backend is not, and passlib is unmaintained. `verify_password` still catches `BaseException`, for the reason CONVENTIONS.md §9 records: one corrupt hash must not 500 every login.
- [2026-09-05] `email-validator` added → it backs `EmailStr`, so a typo is a 422 at the edge instead of an integrity error at the CITEXT unique index. It rejects reserved TLDs, so the test fixtures moved from `@bonvi.test` to `@bonvi.uz`.
- [2026-09-05] The panel's refresh token is an **HttpOnly cookie and never a response body field** (SPEC §4.7) → a token in the body is a token in the JavaScript heap, which is what the cookie was chosen to avoid. `Secure` is set only in production so a LAN `http://` demo still works — the same trade the audio player's `blob:` fallback makes.
- [2026-09-05] `core/ratelimit.py` is an **in-process fixed-window counter** → correct for release 1, which is one uvicorn process on one host (`STACK.md`), and wrong behind multiple workers, where each would keep its own counters. Stated in the module docstring; moving to Redis changes no call site.
- [2026-09-05] `PUBLIC_ROUTES` gained a seventh entry, `GET /i/{code}/apk` → SPEC §4.1 rule 5 names "the install landing page and the APK download" as one public pair, and §8.1 gives the download its own per-agent path so it is the first funnel signal after `invited`. **This is a touch of the permission registry, which is otherwise Phase 6's file**; it is one declaration line with the citation inline, and the alternative was a landing page whose only button 404s.

**Language.**

- [2026-09-05] Alert wording moved to `core/messages_uz.py::ALERT_TEXT`, and the install page's prose to `INSTALL_PAGE` → §14 allows Uzbek in exactly three files, and the first pass had it inline in `calls/service.py`, `installations/service.py` and `enrolment/landing.py`. A service now names the alert kind and never writes the sentence. The landing-page test asserts against the catalogue (HTML-escaped), so polished wording does not break a test and a deleted key does.

**Behaviour the SPEC did not settle.**

- [2026-09-05] `DELETE /calls/{id}` carries **no permission dependency** → T45 asserts 405 for all five roles, so the refusal has to outrank authorisation; a `viewer` getting 403 would make the answer depend on who is asking, and it does not. A caller with no token still gets 401. Both routes are listed in the harness's `AUTHENTICATED_ONLY_ROUTES` with that reason.
- [2026-09-05] A `replaced` installation is **allowed through `require_active_installation`** and refused only on refresh → SPEC §9.3 and §4.3 use the same rule for the same reason: refusing an old binding must never destroy the calls it is still holding.
- [2026-09-05] The install landing page shows **one page for unknown, used, revoked and expired codes** → the enrolment API distinguishes them (404/409/410) because the caller has already presented the code from the app; the public page does not, because the distinction is equally useful to somebody working through codes and the agent's next action is identical.

### Android scaffold decisions (T22)

- [2026-09-05] `WRITE_EXTERNAL_STORAGE` is **not** declared on the `legacy28` flavour, although SPEC §7.2 names it → OEM recordings are opened read-only and never moved, modified or deleted (CONVENTIONS.md §8.3), so a write permission would authorise the one thing the privacy boundary forbids. `READ_EXTERNAL_STORAGE` alone reaches the folder on API ≤28, and every declared permission is another alarming screen inside N40's 15 unaided minutes (T104, R17). `ManifestPermissionsTest` asserts its absence.
- [2026-09-05] `READ_CONTACTS` is not declared, although `contacts` is one of SPEC §3.1's twelve capabilities → the capability exists so the panel can report it; the permission does not, because §8.1 promises in writing that the contact book is never uploaded (N28). On every handset the capability reports `not_applicable`. The same reasoning excludes SMS, location, camera and image media; `ManifestPermissionsTest` fails if any of them appears.
- [2026-09-05] SPEC §7.2 says `modern34` declares the `phoneCall` foreground-service type → **decided: it is declared nowhere; both flavours run on `microphone|dataSync`.** On API 34 that type requires the app to be the default dialer, to hold `MANAGE_OWN_CALLS`, or to be a device owner, and BonviCall is none of the three and **cannot become any of them** — it observes calls rather than managing them, and the handsets are the employees' own so device-owner provisioning is out of scope (`REQUIREMENTS.md` §5.7). Starting an FGS of a type the app does not qualify for throws `SecurityException`, so this was never an M0 measurement; it was a crash on every API 34 device. The reasoning is written into `src/modern34/AndroidManifest.xml` so nobody re-adds it from the SPEC, and `ManifestPermissionsTest` fails if they do.
- [2026-09-05] Cleartext in debug builds → **decided: permitted to NAMED HOSTS ONLY, never blanket.** `src/debug/res/xml/network_security_config.xml` keeps `<base-config cleartextTrafficPermitted="false">` and opens a `<domain-config>` for `10.0.2.2`, `127.0.0.1`, `localhost` and one LAN address. Requiring real TLS for every local iteration is a recurring tax across Phase 4 and the risk is near zero — `src/debug/` resources are not in a release artefact — but a blanket `cleartextTrafficPermitted="true"` is what the rule exists to stop. `ManifestPermissionsTest` asserts that `main` refuses cleartext outright, that no release override exists, that the debug hole is a domain list with no wildcard, and that **no build type trusts user-installed certificates**: a handset with a corporate or malware root must never be able to read the audio of every call the fleet makes.
- [2026-09-05] No Firebase / Play Services dependency in the scaffold, although D-03 allows an FCM data message as the command-socket wake-up → N33 and CONVENTIONS-CLIENT.md §9 forbid a Play Services dependency in the capture or upload path, and the APK is self-hosted onto phones that may have no Play Services at all. AN-CMD adds it for the command path only, and `gradle/libs.versions.toml` says so where somebody would go looking for it.
- [2026-09-05] `data/remote/dto/` is generated but **not yet committed** → `make android-dto` is wired and proven (46 models), and nothing imports the DTOs until AN-NET. The §8.5 blocker is **closed**: `build-backend` replaced `DeviceEventIn.detail`'s free-form map with `DeviceEventDetailIn` (named fields only) and constrained `kind` to a lower-case identifier, and `DeviceVerificationStatusOut.tokens` went the same way. `DeviceContractPrivacyTest`'s exception list is now empty and stays in place: the next free-form map added to a device request fails the build, and having to add a line to that list is the point at which somebody has to justify it.
- [2026-09-05] Kotlin 1.9.24 / AGP 8.5.0 / kapt rather than Kotlin 2.x + KSP → this is the combination `../CallSentry` is proven on, and the whole point of T22 is that Phase 4 can start against something that builds. The KSP migration is real work with a real payoff (build time, and Moshi drops kapt in 2.0) and is recorded as a lint `informational` entry naming T145 rather than silenced.
- [2026-09-05] Android is the one part of the project that builds on the host, not in a container (`make android-build` / `android-test` / `android-lint`) → the SDK is ~2 GB of platforms, build-tools and an accepted licence, and anything on a real phone needs USB. The Makefile says so where the "nothing is installed on the host" rule is written.

### Panel calls pages (T28 / PN-CALLS, M1)

- [2026-09-05] The keyset **cursor is in the URL but the "previous" trail is component state** → a keyset cursor points into a moving stream and only goes forward, so page N−1 cannot be reconstructed from page N and there is no page number to link to. The current page stays linkable (CONVENTIONS-CLIENT.md §2 wants filters and page in the URL); a pasted deep link starts a fresh trail, so "previous" from it returns to page one rather than inventing a history the reader never had.
- [2026-09-05] The list shows **both `started_at` and `received_at`**, though SPEC §5.2 names neither column explicitly → the page is ordered by `received_at DESC, id DESC` (§3.12: devices lie about time). Rendering only the device's clock while sorting by the server's makes a correctly ordered list look shuffled, which is exactly what the first live render on real data showed — one seeded handset was six days out. Two columns make the order explainable and the skew visible.
- [2026-09-05] `useAgentDirectory` (a `GET /agents` read) lives in `modules/calls/api.ts` → `CallResponse` carries `agent_id` and no name, so without it the agent column is a UUID and the agent filter is a box nobody can type into. Agents belong to PN-ENROL; creating `modules/agents/api.ts` now would collide with a unit building in parallel. The hook is marked `TODO(PN-ENROL)` to move. It is enabled only on `can('agents:read')`, which a `sales` user does not hold — verified live: that call is 403 for them.
- [2026-09-05] The filter bar surfaces **nine of SPEC §4.7's seventeen filters** (agent, direction, disposition, call type, has_audio, date from/to, remote number, contact name) → `CallListQuery` is generated and already types all seventeen, so the rest cost one control each whenever they are wanted. `capture_route[]`, `audio_missing_reason[]`, `device_model`, `app_variant` and the duration bounds are the gap report's questions (UC-23), not this page's, and a filter bar nobody can read is worse than one filter fewer.
- [2026-09-05] `/calls/:id` renders a **labelled placeholder instead of an audio player** → N43 needs HTTP Range on an endpoint that requires an `Authorization` header, which `<audio src>` cannot send; the Service Worker bridge (T153) is not built and `GET /calls/{id}/audio` is not in `contract/openapi-panel-v1.json` yet. A plain `<audio src>` would play and then fail to seek, which is worse than a labelled gap. `CallDetailPage.test.tsx` asserts no `<audio>` element exists, so the shortcut cannot be taken by accident later.
- [2026-09-05] Two dev logins, `panel.admin@bonvi.uz` and `panel.sales@bonvi.uz` (password `PanelDemo2026!`, the sales one bound to an agent), plus a second agent "Sanjar Toshev" with their own number and installation, were created in the **dev** database to verify M1 → the seeded `admin@bonvi.uz` exists with `must_change_password=true` and a password nobody recorded, and own-scope narrowing cannot be demonstrated with a single agent. Dev data only; `seed.py` is untouched and `bonvicall_test` is unaffected.

### Capture core decisions (T71a, Wave B)

- [2026-09-05] `CaptureRouter` starts **every supported strategy** and picks the winner at `stop()` in preference order, rather than starting only the first supported one → the two routes are different shapes in time. The OEM harvest is post-hoc (the handset's recorder is already running or it is not; `stop()` looks for a file afterwards) and `MediaRecorderStrategy` is live (it must be running during the call or there is nothing to collect). You cannot know which will succeed until the call is over, so the app's own recording runs as **insurance** and is discarded when the OEM file exists. That is CallSentry's behaviour, and S1 confirmed the ordering.
- [2026-09-05] `RecordingStrategy.route` is a **getter, not a constant** → `MediaRecorderStrategy` is one class trying three audio sources, and the source that succeeded is what it reports. S1 is why: `VOICE_RECOGNITION` captures the far end on Samsung and some others and `MIC` does not, so a fleet whose recordings all came from `MIC` is half-deaf and a per-model table saying only "audio: yes" could not tell it from a healthy one. Before a successful start the route reports `app_mic`, the pessimistic answer — overstating what was captured is the failure the gap report exists to catch.
- [2026-09-05] `VOICE_CALL` is never tried, although it is the best source → it is forbidden to non-system apps, so attempting it costs a `SecurityException` per call and gains nothing. `AudioSource` has no such member and a test asserts it.
- [2026-09-05] `CaptureRouter.Outcome` requires **exactly one** of a file or a reason, enforced in an `init` block → the two fields the panel reads must never disagree. A call with both, or with neither, is a row nobody can interpret.
- [2026-09-05] `PermissionCaptureCapabilityChecker.oemRecorderReachable()` returns **false** until T62 → wrong in the conservative direction, and the direction is argued rather than assumed: claiming the handset's own recorder works when it does not makes the OEM strategy run, find nothing and report `attribution_failed`, which says "the privacy boundary refused this recording" when the truth is "the recorder was switched off". Those are two different conversations with an employee.
- [2026-09-05] The privacy boundary is **split across two files** — `domain/PrivacyBoundary.kt` (types and the pure `SubscriptionRule`) and `capture/PrivacyBoundary.kt` (the one Android adapter) → `CONVENTIONS.md` §8.1 puts the file under `capture/` and SPEC §7.1 lists the types under `domain/`. Both are satisfied: the `grep` check for `SubscriptionManager`/`PHONE_ACCOUNT_ID` returns exactly one file, `capture/PrivacyBoundary.kt`, and every branch of the rule is unit-tested without a phone.
- [2026-09-05] `SubscriptionRule` rejects with `INCOMPLETE_CALL` when a call has no end time → a window needs both ends, and a window with an invented end is a window that can reach a file it should not. `RejectReason` is a wire-shaped enum so the call-log delta can count each cause separately (SPEC §4.4).
- [2026-09-05] The call state machine's transition function is **pure and lives in `domain/`**, while SPEC §7.5 says the machine lives in `service/` → the service owns the instance and the persistence, which is what §7.5 is about; the transition rule is pure so its branches are testable without a phone. An event the machine cannot accept returns the current state plus an `IllegalTransition` rather than throwing: during a real call an unexpected event is common, and throwing would lose a call that is otherwise fine.
- [2026-09-05] `CallSessionManager` keys machines by **call id**, not a single "current call" → SPEC §7.5 and **R5**: a second RINGING during an active call is call waiting and is a separate call. The prototype ignored it and the second call vanished. Every transition is written before it is acted on, so a mid-call process death resumes rather than restarts; a terminal call's session row is deleted because the record of it then lives in the upload queue, which is the queue of record (N8).
- [2026-09-05] `CallState` is stored in Room by **name, not ordinal** → an ordinal silently re-points every stored row when a state is inserted in the middle of the enum, and what it would re-point is a queue of calls that have not reached the server. An unknown name resolves to `IDLE`, which the resume pass treats as "start again".
- [2026-09-05] `UploadPolicy` parks on 426 `app_version_unsupported` rather than dropping → `CONVENTIONS.md` §4.4: the order is the rule. An old client drains its backlog first and is refused afterwards, and refusing an old version must never destroy data. Parking keeps every row for the update.
- [2026-09-05] Room DAO behaviour is covered by pure tests of `UploadPolicy` and by fakes behind `CallSessionStore`, not by Robolectric → the rules worth testing are rules, and a rule that can only be tested by writing rows to a database is a rule nobody tests. Real DAO coverage belongs in instrumented tests with the rest of the on-device work.
- [2026-09-05] **T71b (`AN-HARVEST`) is not built**, on instruction: it is the one Android unit genuinely blocked on S1 + M0. `NoOpOemRecordingLocator` returns null in both flavours, which is the fail-closed answer — a placeholder that captured everything by default would be the exact failure the capture package exists to prevent.

### From build-backend, Phase 3 Waves B–F, 2026-09-05

- [2026-09-05] `DeviceEventIn.detail` was `dict[str, str | int | bool | None]` → **replaced by a named `DeviceEventDetailIn` with `extra="forbid"`** → the value types were constrained but the **keys** were not, so an arbitrary pair could leave a handset the employee owns. §8 rule 5 and §15 forbid exactly that, and §8 enforces the same boundary on the Android side with a *type* so it cannot be widened in one line. `tests/test_contract_privacy.py` now walks every device request schema in the contract artefact and fails on any `additionalProperties`; its exception list is empty. `DeviceVerificationStatusOut.tokens` was tidied the same way (server→device, so not a privacy question — a named schema just generates a usable Kotlin DTO).
- [2026-09-05] Modules bind `from src.core import clock` and call `clock.now()`, never `from src.core.clock import now` → **`frozen_clock` patches `core.clock.now`, and a module that has already bound the name keeps the real one.** Three silence-detection tests passed against the wall clock before this was found, which means they tested nothing. Pinned by `test_nothing_binds_the_clock_function_directly`.
- [2026-09-05] Each authenticated test client gets **its own application instance** → `dependency_overrides` is per application, so sharing one meant the last role fixture requested won for every client. It made a four-role DELETE test assert 405 four times as one role, and made a device client authenticate as an admin. Both reported safety they were not testing.
- [2026-09-05] `audio_root` is an **autouse** fixture that also patches `settings.audio_storage_path` → §13 asks for isolation by construction; without the settings patch the service and the factory disagreed about where a file was, and no test could reach the real volume only because none tried.
- [2026-09-05] `conftest.py` rebuilds the test database when the **PostgreSQL enum labels no longer match `core.enums`** → the licence to edit revision 001 (above) has a sharp edge: `upgrade head` on a database already stamped 001 does nothing, so an edited baseline silently leaves the old schema. Two added enum values produced `invalid input value for enum audit_action` across a whole module's tests. The check goes away when 001 freezes at W03.
- [2026-09-05] UC-20 says audio without a token returns "401 and **zero bytes**"; N35 says every non-2xx carries the envelope → **the envelope wins**; the criterion is about *audio* bytes and none leave. A literally empty 401 would also break the Android client, which parses one envelope shape and cannot distinguish an empty body from a truncated one.
- [2026-09-05] `DELETE /calls/{id}` and `/audio` carry **no permission dependency** → T45 asserts 405 for all five roles, so the refusal outranks authorisation; a `viewer` getting 403 would make the answer depend on who is asking.
- [2026-09-05] Per-device data accounting counts the **request** body only (`modules/devices/usage.py`) → N14's 1 GB cellular cap is essentially all audio upload, where a ~512 KB chunk meets a ~1 KB response; the error is a fraction of a percent and it under-counts responses rather than over-counting the employee's bill. Stated in the module docstring so the assumption is visible if a large download to the phone is ever added.
- [2026-09-05] The device `config` block is an **allow-list** (`core/settings_keys.py::DEVICE_VISIBLE_KEYS`), not the settings table → a handset has no business knowing the retention period or who receives the alert e-mails.
- [2026-09-05] `seed.py` **generates and prints** an admin password once when `SEED_ADMIN_PASSWORD` is unset outside production, and refuses in production → the first seeded admin had a password nobody had recorded, so the account could not be used. The password is printed to stdout and never logged (N26); the account is created with `must_change_password=True`.
- [2026-09-05] `CallResponse` carries `agent_name`, `number_e164`, `device_model` and an `audio` sub-object → resolving a name per row in the browser is the N+1 problem relocated, and the `device_model` filter existed server-side for a column the panel could not render. `capture_route` is in the payload because `S1-RECORDING.md` requires `app_voice_recognition` and `app_mic` to stay distinguishable — the per-model capture rate is the M0 baseline and UC-23's regression alert reads it.
- [2026-09-05] `PUBLIC_ROUTES` has seven entries: the five of SPEC §4.1 rule 5, plus `POST /api/device/v1/enrolment/redeem` (§4.2, cannot carry a token) and `GET /i/{code}/apk` (§4.1 rule 5 names the download alongside the page; §8.1 gives it a per-agent path so it is a funnel signal).
- [2026-09-05] `core/reads.py` corrected: `CallAudioModel` is listed under `audio`, not `calls` → §2.1's draft named the wrong module and the AST check compares against where the class actually lives. `gaps` additionally reads `devices.DeviceModel` (per-model capture rate) and `settings.AppSettingModel` (the regression threshold); both are in the declaration.
- [2026-09-05] T55's command **lifecycle** is complete (issue, collect, acknowledge, latency, timeout, expiry, and "no call is ever linked to a failed command"); the **WebSocket hub and the FCM wake-up are not built** → the lifecycle does not depend on which transport delivered the command, so it could be built and tested first. The device currently collects by polling `GET /api/device/v1/commands`, which is the documented fallback path of SPEC §4.6 and not a substitute for the socket where UC-16's five-second bar is concerned.
- [2026-09-05] Client asked to remove the TV board, the work-numbers page and the enrolment funnel -> Removed all three from the panel; the *capabilities* survive and move to the agent detail page (register a work number, issue an enrolment code) -> For a 15-person team, registering a number and issuing a code are things you do TO AN AGENT, so three top-level sections were navigation overhead rather than structure. Deleting the pages without moving the capability would make the product unusable: no number, no enrolment code, no device. All three were still stubs, so nothing built was lost
- [2026-09-05] The `viewer` role and `monitor:read` no longer have a page -> Remove the role, the permission and the `/monitor/board` endpoint server-side -> The TV board was the only surface `viewer` could see. A role that can log in and reach nothing but 403s is worse than no role: it looks like a permissions bug to whoever is handed the account

### Scope change: the `viewer` role removed, 2026-09-05

- [2026-09-05] The client dropped the TV board, the work-numbers page and the enrolment funnel from the panel (`c1146c1`) → **`viewer` and `monitor:read` are removed from the server as well.** The board was the only surface that role could reach, and a role that can log in and then 403s on everything reads as a permissions bug to whoever holds the account — which somebody eventually "fixes" by granting more than they meant to. **`core.permissions.Role` is now four** (`admin`, `manager`, `sales`, `service`) and **`core.enums.UserRole` is three** (`service` is a `service_tokens` row, not a login — SPEC §3.2). `viewer` was also removed from the `user_role` PostgreSQL enum by editing revision 001, which is still permitted until W03.
- [2026-09-05] **No endpoint was deleted.** `POST /numbers`, `POST /numbers/{id}/assignments`, `POST /numbers/{id}/enrolment-code`, `GET /enrolment/attempts` and `GET /enrolment/receiver-status` all stay: registering a number and issuing a code are things done *to an agent*, and only the navigation moved. `GET /monitor/board` and its masked DTO were never built — the TV board was Phase 5 work — so there was nothing to remove there.
- [2026-09-05] Every test that asserted a `viewer` refusal now asserts the same refusal for the role that holds the least access on that route (`sales` or the `service` token) → the assertions were about "a role without this permission is refused", and that property still needs testing. Nothing was deleted to make the suite pass.

### Robustness: an impossible call must not 500 (found by `scripts/demo_data.py`)

- [2026-09-05] A call whose `direction`/`disposition` combination violates `ck_calls_direction_disposition` returned **500** → **two changes.** The rule now lives in `calls/rules.py::is_valid_combination` and is checked by a `model_validator` on `DeviceCallIn`, so a device gets a **422 naming the item index** and can park the record (N9) instead of retrying an impossible one forever. Separately, each batch item is now wrapped in a **savepoint**, so any constraint the schema did not foresee fails that item alone rather than poisoning the transaction and losing the other forty-nine (SPEC §4.4 rule 6).
- [2026-09-05] **Residual tension worth naming:** a *batch* containing a schema-invalid item is still rejected wholesale by Pydantic, because request-body validation happens before the handler. The 422's `detail.fields` names the index (`body.calls.3.disposition`), so the client can drop that item — but it is not per-item in the way rule 6 means. Moving the combination check out of the schema and into the service would make it per-item at the cost of the contract no longer documenting the rule. Flagged for a decision; the current shape favours the documented contract.
- [2026-09-05] The per-item failure message carries **the constraint name only**, never PostgreSQL's error string → the DETAIL line contains the whole failing row, which for a call includes the number the employee dialled. A test asserts the customer's number does not appear in the response.
- [2026-09-05] The `credential_replay` alert is raised **after** the savepoint rollback → SPEC §3.10 rule 5 wants both halves true: nothing about the call is stored, and the alert is. Raised inside the savepoint it was rolled back with the item, and two devices claiming one call passed in silence. Caught by an existing test when the savepoint was introduced.
- [2026-09-05] `password_generated` and `password_required` added to the log redactor's exceptions → the substring rule turned a boolean flag into `password_generated=…`, which hides a useful fact and teaches people the log line is noise. Over-redaction is its own failure mode.
- [2026-09-05] `SEED_ADMIN_EMAIL` / `SEED_ADMIN_PASSWORD` added to `.env.example`, plus a `make seed` target → the demo could not log in after a database rebuild because no admin existed and the seed had never been run as part of any documented flow.

### Panel Wave A — the rollout on `/agents/:id` and `/devices` (2026-09-05)

- [2026-09-05] `/devices` is driven by **`GET /installations`**, with `GET /devices` merged over the top, rather than by `/devices` alone → `DeviceService.list()` inner-joins `device_health`, so an installation that was bound and never sent a heartbeat is absent from that response and `GET /devices/{id}` answers 404 for it. That is the most alarming phone in the fleet (R3 caught one step earlier), and absence is the signal, so absence cannot be how it is rendered. `buildFleet()` gives it the state `never_reported` and sorts it above `offline`. Pinned by `modules/devices/__tests__/fleet.test.ts`, which fails if anyone "simplifies" the page back to reading `/devices`. **The real fix is a LEFT OUTER JOIN server-side; this merge should be deleted when that lands.**
- [2026-09-05] The device list's default order is **worst first** — never reported, offline, degraded, revoked, healthy, and within a state longest-unheard-from first → a fleet page sorted by name makes one broken phone out of fifteen exactly as prominent as the fourteen working ones, which is the same as not having the page.
- [2026-09-05] `useAgentAssignments` **fans out one request per registered number** → there is no `GET /assignments?agent_id=` and no `GET /agents/{id}/assignments`, so the only way to answer "which line is this person's, and which did they hold before" is to read every line's history and filter. At fifteen numbers that is fifteen small cached requests; at three hundred it is not, and the fix is one server-side filter rather than anything in the panel. Closed assignments are kept deliberately: calls from before a handover still belong to the previous holder (D-08), and a page showing only the open assignment would be lying by omission on exactly the day somebody asks why last month's calls are filed under another name.
- [2026-09-05] `GET /enrolment/receiver-status` is consumed through a **validating parse**, not a generated type → the route is declared `-> dict[str, str | bool]` on the server, so it reaches the contract as `additionalProperties` and `types.gen.ts` has nothing to offer. That is a `CONVENTIONS.md` §15 violation server-side ("`Dict[str, Any]` in a wire schema"). `parseReceiverStatus` returns `null` on an unexpected body and the banner degrades to silence rather than asserting a shape that may have drifted; it is deleted the day the route grows a `ReceiverStatusResponse`. The banner itself is not optional — if the receiver is down nobody can enrol, and SPEC §5.2 requires the page to say so before anyone tries.
- [2026-09-05] The enrolment section renders `FUNNEL_STAGE_HINT` — **an instruction, not the stage name** → "permitted" tells an admin nothing they can act on; "ruxsatlar berildi, endi raqamni tasdiqlash kerak" does. R17 is the project's top practical risk and its expensive failure mode is silence, so the page is written to be read by somebody who is *not* on the phone with the agent: where they are, what happens next, and what went wrong last.
- [2026-09-05] `CloseAssignmentModal` asks for the end **date** and defaults to today rather than sending "now" → the common real case is an admin recording a handover that happened last Friday, and the server re-attributes calls from the instant given. Silently using `now` would file four days of somebody else's calls under the wrong name with nothing on any screen saying so. Sent as `T00:00:00+05:00` so the boundary lands on the Asia/Tashkent calendar day the admin picked.
- [2026-09-05] `sim_owner` is **asked** in the assign-number modal rather than defaulted → the answer decides whether a recording is Bonvi's to keep at all (R10): a call on an employee's personal SIM is a different legal position from one on a company line. It is a local constant because the server declares the field as a string with a `^(company|employee)$` pattern rather than an enum, so `types.gen.ts` offers only `string`.
- [2026-09-05] `useAgentDirectory` moved from `modules/calls/api.ts` to `modules/agents/api.ts`, resolving the `TODO(PN-ENROL)` left by PN-CALLS → every rollout page turns an `agent_id` into a name, and four copies of that lookup would be four things to fix when the roster endpoint grows a cursor. The calls LIST no longer uses it for rows at all: `CallResponse.agent_name` is resolved server-side now, and the roster is fetched only to fill the agent filter's option list.
- [2026-09-05] `Perm.MONITOR_READ` is left in `shared/auth/permissions.ts` although the board and the `viewer` role are gone → the file is a mirror of the server registry and `build-backend` is still removing the role; deleting the constant ahead of them would make the mirror wrong in the other direction. It is referenced by nothing.

### The scheduler and the periodic jobs (T150, T151, T152), 2026-09-05

- [2026-09-05] Jobs hold a **PostgreSQL advisory lock**, not a row lock or a table flag → an advisory lock is released when its session dies, so a worker killed mid-job leaves nothing for a human to clear. A second worker that cannot take it **skips** rather than queues: a queued duplicate runs the job twice in a row, which is the thing being prevented, only later.
- [2026-09-05] **Every job is idempotent as well**, and that is not belt-and-braces → the lock does not protect against the case that actually happens, which is a worker restarting after a crash and running the same job again. Proven live: two workers with ticks 3 s apart both ran `command_timeout`, and that is harmless only because it is idempotent.
- [2026-09-05] `audio_retention` deletes the blob **first** and marks the row **second** → a crash between them leaves a row claiming a file that is gone, which the next run repairs because `delete` is idempotent. The other order leaves a file nobody deletes and nobody can find — the failure that running the job again cannot fix.
- [2026-09-05] `silence_detection` runs every five minutes **including at night**, and the working-hours arithmetic is what keeps it quiet → skipping nights in the cron expression instead would delay a real alert until well after Monday morning, and a holiday added to the settings would then need a redeploy to take effect. Verified live at 03:29 Tashkent against the demo fleet's deliberately silent handset: the job ran and raised nothing.
- [2026-09-05] The "three failures raise an alert" policy moved from `core/jobs.py` to `worker.py` as an injected `on_repeated_failure` → `core` must not import a module (§2), and the policy belongs to the composition root rather than to "run a function under a lock". Caught by `test_core_imports_no_module_except_the_registry`.
- [2026-09-05] `model_capture_stats` is **computed by `gaps` and written by `catalog`** → `gaps` holds the §2.1 read exception and rule 2 says a holder never writes; `catalog` owns `model_capture_stats`. Caught by `test_cross_module_read_holders_never_write`.
- [2026-09-05] A capture rate over fewer than **10 answered calls** never raises N4's regression alert → a model with four calls at 50 % has told us nothing, and alerting on it trains people to ignore the alert that matters.
- [2026-09-05] `core/jobs.py` and `seed.py` call `database.get_engine()` through the module → the same trap as `clock.now`: three lock tests passed alone and failed in a suite because the pooled engine was bound to the first test's event loop. `tests/test_layering.py::test_nothing_binds_a_patchable_singleton_directly` now covers the clock and both database singletons.
- [2026-09-05] The suite **empties the test database at session start** → per-test rollback keeps runs independent while everything behaves; a fixture whose wiring is wrong commits on its own connection and the next run fails somewhere unrelated with a count one too high. That happened (eleven unrelated failures from one leaked alert row), and diagnosing it from the failure is genuinely hard, so the suite now starts from empty by construction.

### From the panel's Wave A, 2026-09-05

- [2026-09-05] `GET /devices` inner-joined `device_health`, so **an installation that never reported was absent from the list and 404 on the detail page** → now a LEFT OUTER JOIN, with `never_reported` distinct from `is_online: false` and sorted first. It is the device an admin most needs to see, the endpoint could not represent the strongest form of absence, and it failed in the reassuring direction — the fleet looked smaller and healthier than it was. **The same blind spot was in `sweep_offline` and `detect_silence`**: the one phone nobody had ever heard from raised no alert at all, which is the exact inversion of what a sweep is for. Both fixed; verified live (5 offline alerts, one flagged `never_reported`). It surfaced only because the demo fleet has a deliberately silent handset — a uniform set of healthy fixtures would have shipped it.
- [2026-09-05] `GET /devices/{id}` declared the **list** response model, silently stripping `capabilities` and `capturing` → the two things the device page exists to show. Found while fixing the join.
- [2026-09-05] `GET /enrolment/receiver-status` returned an untyped `dict` → now `ReceiverStatusResponse`. §15, and it is the one endpoint whose failure means nobody can enrol, so it should be the best-specified thing on the page rather than the least.
- [2026-09-05] `GET /assignments?agent_id=` added → the agent detail page was fanning out one request per number; fifteen cached round trips are fine and three hundred are not.
- [2026-09-05] `CallAudioSummary.url` added — a **path**, present only when the recording is available, never signed and never public (N20) → the route is token-protected and the browser reaches it through the Service Worker bridge, which is the only way an `<audio>` element can carry an `Authorization` header and still issue real Range requests (T153, N43). The player and the bridge land together.

### Panel — the audio bridge and the oversight surfaces (2026-09-05)

- [2026-09-05] The panel workaround that merged `GET /installations` into `/devices` is **deleted**, and the rule it protected is kept → `DeviceHealthResponse.never_reported` is a first-class field now and `GET /devices` LEFT OUTER JOINs, so the merge is dead weight. `never_reported` still sorts above `offline` and `fleet.test.ts` still fails if that changes: "was handed a phone and never started" is a person waiting for help right now, "worked once and stopped" is not. The workaround went; the invariant did not.
- [2026-09-05] `useAgentAssignments` is one request again → `GET /assignments?agent_id=` exists, so the fan-out over every registered number is gone. Closed assignments are still kept deliberately (D-08).
- [2026-09-05] `parseReceiverStatus` is **deleted** → the route has a real `ReceiverStatusResponse`, so the defensive parse is replaced by the generated type and a field rename is a compile error again.
- [2026-09-05] `resolveAudioUrl` **ignores an absolute `audio.url` and falls back to the canonical same-origin path** → N20 says the URL is a path, but if one ever arrives absolute, using it would be cross-origin, and cross-origin makes the browser withhold `Content-Range` from both JS and the Service Worker. The cost is silent: the player still plays and seek simply stops working, with no error anyone can see. Pinned in `audio.test.ts`, including the protocol-relative `//host` form.
- [2026-09-05] The Service Worker token handoff **waits for an acknowledgement** rather than firing and forgetting → `postMessage` is asynchronous, so without the round trip the first play can leave before the token lands and return 401, which `<audio>` surfaces only as a generic `error` event with no status attached. Two-second timeout, then the blob fallback.
- [2026-09-05] `src/modules/calls/audio.ts` is exempted from the bare-`fetch` lint rule, and the exemption block sits **after** the `src/modules/**` block that re-declares it → this is the exception CONVENTIONS-CLIENT.md §4 already anticipates: a binary stream carrying a `Range` header whose `Content-Disposition` must be read off the response. Routing it through `api.get` would mean teaching the JSON client about blobs and byte ranges to satisfy a rule about JSON.
- [2026-09-05] `/alerts` derives its wording from `AlertKind`, **not** from the server's `title_uz` / `body_uz` → on the live server those read `"Device offline"` (English, a §14 violation for a string an end user sees) and `"Xatolik yuz berdi. Administratorga murojaat qiling."` (the generic fallback, which says nothing about the alert). A page built on them would be a list of identical shrugs. `ALERT_KIND_LABEL` and `ALERT_KIND_HINT` are `Record<AlertKind, MessageKey>`, so a kind added on the server is a compile error rather than a blank row.
- [2026-09-05] Every alert carries a **next action** and a route to the thing that can fix it, chosen by `ALERT_TARGET` → a link to a handset is a dead end for a storage, receiver or retention-job alert, and an alert nobody can act on trains people to scroll past the next one.
- [2026-09-05] The gap report **displays the server's percentage strings verbatim** and never re-rounds them → the columns are NUMERIC and arrive as `"82.40"`. Re-rounding on the way to the screen is how a report ends up disagreeing by a tenth of a point with the call list UC-22 requires it to reconcile with. `percentValue` parses only for comparison and bar widths.
- [2026-09-05] An absent `baseline_rate` renders blank, not as a pass → T14 has not measured M0 for most models, and "no baseline" is not "no regression".

### Enrolment flow decisions (E1–E6)

- [2026-09-05] **`installed` is never rendered as `done`.** Redeeming a code returns only a *provisional* token; the real access/refresh pair is issued after the handset proves it is on the registered number, and until then the server accepts no calls. `EnrolmentViewModel.UiState.isFullyEnrolled` is the only thing E6 keys off, and it is `CaptureReadiness.evaluate` — the same function the server-facing capture state uses, so there is no second code path that could disagree (UC-03's never-false-ready). E6 renders the blocking capability by name rather than "Tayyor" when it is not true.
- [2026-09-05] Every E2 row turns green only when the **capability check passes**, never when the dialog was dismissed → the `RequestPermission` launcher's result is deliberately discarded and `CapabilityChecks.check()` runs instead. `granted_not_working` gets its own sentence and sends the agent to the OEM's settings screen, because retrying the system dialog will never fix it; `denied_permanently` gets "Sozlamalardan yoqing"; `denied` gets the dialog. Three states, three different next actions, because they are three different problems.
- [2026-09-05] `MicrophoneProbe` treats an **all-zero one-second capture as `granted_not_working`**, not as success → that is the OEM-blocking signature: the API succeeds and the audio is silence. A quiet room is not digital silence. Nothing is written to disk; the buffer is inspected in memory.
- [2026-09-05] `oem_autostart` and `oem_recorder` report `UNKNOWN`, never `granted` → SPEC §8.2: a false green there is exactly how R3 stays invisible. Neither is in `CaptureReadiness.REQUIRED`, so an unverifiable capability cannot block a working phone either.
- [2026-09-05] The six screens share **one ViewModel**, scoped to the enrolment nav graph → they are one transaction: E2's results decide whether E6 may be reached, E4's SIM choice is what E5 verifies, and E1's number is on every screen after it. Six ViewModels would mean passing that state between destinations, which is how a half-finished enrolment ends up looking finished.
- [2026-09-05] N41 is a component (`NumberBanner`) on **every** enrolment screen, fed from `SessionStore` and written the moment the code is redeemed → true before the install finishes, not after, and it survives a restart mid-flow because the ViewModel reads it back on init. The install landing page already said the same sentence before anything was installed; this is the app keeping the promise.
- [2026-09-05] E5 never tells the agent that route 1 failed → SPEC §8.2: "the agent is never told the app 'checked the SIM and failed'; they are told what to do next". Route 1 runs behind a spinner with no text about SIMs; if it does not prove the match the screen becomes the callback route.
- [2026-09-05] `no_caller_id` offers **"Administratorga murojaat qilish", not "retry"** → R19: the operator withheld CLI and retrying can never work on that operator. The button posts `enrolment_stuck`, which moves the agent to `needs_assisted_install` in the panel. A retry button there would be a lie that costs a person the rest of their fifteen minutes.
- [2026-09-05] The `contacts` capability is reported `NOT_APPLICABLE` and its permission is never requested → the capability exists so the panel can report it (SPEC §3.1); the permission does not, because §8.1 promises in writing that the contact book is never uploaded. The E2 row still appears, marked *ixtiyoriy* and skippable, so the promise is visible rather than merely kept.
- [2026-09-05] `ui/` may not import `capture/`, and `MainActivity` did → caught by `ArchitectureRulesTest`, fixed with a `domain/SimDirectory` seam rather than by relaxing the rule. `capture/` keeps the only reader of `SubscriptionManager` (CONVENTIONS.md §8.1) and exposes the SIM list as pure data.
- [2026-09-05] `Capabilities` version predicates carry `@ChecksSdkIntAtLeast` → it is what makes "ask a capability, never a version" (CONVENTIONS-CLIENT.md §7) compatible with lint's `NewApi`. Without it the only options are a raw `SDK_INT` at the call site, which `ArchitectureRulesTest` forbids, or a blanket `@SuppressLint`, which forbids nothing.
- [2026-09-05] Lint's `BatteryLife` (a Play Store Content Policy check on `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`) is `informational` → the APK is never distributed through Play (N33), and UC-05 requires the exemption outright: without it the OEM battery manager stops the capture service overnight and the phone silently records nothing.

### Contract findings from the phone (2026-09-05)

- [2026-09-05] **Every 64-bit wire integer is declared without `format: int64`.** Pydantic's `int` is unbounded, so it reaches OpenAPI as a formatless `integer` and openapi-generator maps that to 32-bit `kotlin.Int`. `System.currentTimeMillis()` is ~1.77e12 against an `Int.MAX_VALUE` of 2.1e9; `free_storage_bytes` on a 128 GB handset is 1.28e11. Nine fields overflow: `device_epoch_ms` on `DeviceRedeemIn`/`DeviceCallIn`/`DeviceHeartbeatIn`, and the byte counters on `DeviceHeartbeatIn`/`DeviceEventDetailIn`. The server stores them all as BIGINT, so the **data model is right and only the wire schema is under-specified**. Stopgap: `android/scripts/widen_int64.py`, machine-applied and listed. `Int64WireContractTest` fails when a listed field gains the format — that is the signal to delete the entry, and the script with the last one. Server fix: `Int64 = Annotated[int, Field(json_schema_extra={"format": "int64"})]`.
- [2026-09-05] **`IssuedTokensOut` carries no `verification_method`.** SPEC §9.3 requires an attested binding to be "rendered differently from `number_verified` everywhere it appears", and E6 is the single most important place it appears — yet the response that completes verification does not say how the number was proven. `DeviceTokenPairOut` (the refresh response) does carry it. Worked around by reading `VerificationState.attested` from `DeviceVerificationStatusOut.state` and persisting the method locally; that is enough for E6 but means a client that only ever saw a refresh would be relying on the persisted copy. Adding the field to `IssuedTokensOut` would remove the inference.

### Demo audio and the realtime socket (2026-09-05)

- [2026-09-05] Demo audio: real bytes or a random blob? → **a genuinely decodable Ogg Opus file**, built by hand in `server/scripts/synthetic_audio.py` → the server never decodes audio, so a blob would exercise the whole chain equally; but the panel's player would then fail to open every demo recording, and "the player is broken" and "the demo audio is fake" look identical on screen. Verified by decoding with libsndfile 1.2.2, not by inspection.
- [2026-09-05] The silent Opus packet is `f8fffe`, lifted verbatim from libsndfile output, **not** a bare TOC byte → RFC 6716 §3.4 permits a zero-length frame and it looks like the tidier construction, but libopus rejects such a stream at open. This is recorded in the module so nobody "simplifies" it back.
- [2026-09-05] Demo calls are capped at 300 s and their audio matches the true call length, rather than capping the audio → a capped clip against an uncapped call trips `audio_duration_mismatch` on *every* call, which makes a working system look broken. One call is deliberately mismatched so the warning still has a real example, and one is 400 days old so `audio_retention` has something to expire. Whole fleet: 23 recordings, 687 KB.
- [2026-09-05] `make demo` now runs `make job n=audio_retention` afterwards → the panel's "expired" state is produced by the real job rather than an UPDATE, so the demo proves the job works while it is seeding.
- [2026-09-05] `make demo-reset` deletes the audio blobs as well as the rows → `reset_demo.sql` is SQL and cannot reach the filesystem; leaving them accumulates files no row points at, which is the one state the retention job can never clean up.
- [2026-09-05] `make lint` now covers `scripts/` → it was the only Python in the repo outside the linter, and it is the file that doubles as the smoke test. Four pre-existing violations were fixed in the same pass.
- [2026-09-05] `get_current_principal` takes `HTTPConnection`, not `Request` → the socket authenticates on its handshake (SPEC §4.6) and must go through the *same* code as REST. A second authentication path is how one of them ends up missing a check. `Request` and `WebSocket` are both `HTTPConnection` and the resolver only reads headers.
- [2026-09-05] The `config` frame of SPEC §4.6 is **not** implemented → config already reaches the phone on the heartbeat response (`config_hash` + body only when changed, §4.5), and a `command` of kind `config` already tells a phone to go and look. Adding a frame that carries a free-form settings map would be a third delivery path for the same data. T55 is the command channel; the frames it names are built.
- [2026-09-05] A frame outside the allow-list **closes** the socket (1003) rather than being ignored → silently discarding frames makes an app on a different protocol version look like a phone that ignores its commands, and those need different fixes. The app's 1 s → 5 min backoff covers the reconnect.
- [2026-09-05] WebSocket frames are exported as `contract/device-ws-frames.json` → OpenAPI cannot describe a WebSocket, so without it the socket's frames would be the one part of the wire hand-written on both sides — the case CONVENTIONS.md §1 forbids, on the one channel where a mismatch means a phone silently never dials.
- [2026-09-05] `set_ws_connected` updates an existing `device_health` row and never creates one → inventing a row of nulls would put a phone into the health table before it has ever reported any health, which is exactly the state `detect_silence` reads as "never reported". The hub is the live answer to who is connected; the column is the panel's copy.

### Capture pipeline decisions (T69, T68, T74, T67, T70)

- [2026-09-05] **Two call-detection sources, not one** → a manifest-registered `PHONE_STATE` receiver AND a `TelephonyCallback`/`PhoneStateListener` registered by the service. The receiver arrives when the process is not running, which is the case that matters: the first thing that happens after a reboot is often a call, and a phone that has not been opened since is still taking them. Both funnel into one `CallDetector`, so the rules exist once and are unit-testable.
- [2026-09-05] **The privacy boundary runs at DETECTION.** Guard 1 evaluates before any field is written; a call on an unregistered or unidentifiable SIM never enters the queue and leaves only a counter — no number, no time, no duration (N28). The recovery sweep applies the same filter to call-log rows, because the log holds the employee's private calls and a sweep that queued everything would upload what the live path correctly refused.
- [2026-09-05] `client_call_id` is **UUIDv5 written out by hand**, not `UUID.nameUUIDFromBytes` → that method is version 3 (MD5) and would silently disagree with the server's `uuid.uuid5`, uploading every recovered call a second time. `ClientCallIdTest` pins the output against a value produced by Python's `uuid.uuid5`, which is the only thing that makes N2's "duplicate rate must be 0" checkable from this side.
- [2026-09-05] The record is **not built when the call ends** → it is built by the sweep twenty seconds later, because the call-log row appears after the call and §3.10 rule 1 requires the id to come from the log's `DATE`. Building it early would mean a correction on every call rather than a right answer on most.
- [2026-09-05] `CallDetector` does not touch WorkManager; it calls an injected `CallEndedListener` → a class that touches WorkManager cannot be unit-tested, and the branches that would have lost their tests are the privacy-boundary ones. Found by a test failure, fixed by a seam.
- [2026-09-05] Four survival mechanisms, all of them (UC-05) → `START_STICKY`, `BOOT_COMPLETED`/`LOCKED_BOOT_COMPLETED`/`MY_PACKAGE_REPLACED`, an `AlarmManager` one-shot from `onTaskRemoved` (the only thing that runs after the process is gone when the user swipes the app away), and a 15-minute `WatchdogWorker` for "the OS stopped it and told nobody". The watchdog starts nothing before an installation exists — a notification on the phone of somebody mid-enrolment is the first thing they switch off.
- [2026-09-05] The upload worker retries rather than fails on a transient problem → a failed worker is never rerun, and the row it was carrying would sit in the queue until something else happened to schedule a drain. A one-shot on enqueue gives "within minutes"; the 15-minute periodic is the net for when the process died before scheduling it.
- [2026-09-05] A batch is confirmed **per call**, not per response → `POST /calls` accepts partially, and a call the server rejected must keep its row rather than vanish with the batch. A call the server did not mention at all also stays queued: dropping it would lose a call on a server bug.
- [2026-09-05] `CallRecordBuilder.reasonFor` guarantees an `audio_missing_reason` on every path (T75, N5) → an unanswered call is `not_expected` and a captured one `pending_upload`, both excluded from the gap report's denominator (SPEC §3.9), so a healthy fleet does not read as broken. With the OEM locators still `NoOp`, the honest fallback is `recording_route_unavailable` — a complete, correct record that names the right cause.
- [2026-09-05] `isReturnDefaultValues = true` for unit tests → the only Android call on the detector path is `SystemClock.elapsedRealtime()`, and returning 0 makes durations deterministic instead of dependent on wall-clock luck. Anything that genuinely needs Android behaviour belongs in an instrumented test.
- [2026-09-05] The call-log SIM-attribution column is named **only** in `capture/PrivacyBoundary.kt`, as `SIM_ATTRIBUTION_COLUMN` → `CallLogReader` projects it by reference and carries the value as opaque text; only the boundary interprets it. The constant deliberately does not echo the platform's name, so CONVENTIONS.md §8.1's grep check stays literally true and the name says what the column is for.

### Distribution and session decisions (T81, T79, T83, T80)

- [2026-09-05] The `int64` workaround is **deleted**. `build-backend` annotated the nine fields `format: int64`, `Int64WireContractTest` went red on the next run exactly as designed, `android/scripts/widen_int64.py` was removed and the generated DTOs now carry `kotlin.Long` unaided. The test stays with an empty list: its value now is catching the next one, and the last case in it fails on any new formatless `*_epoch_ms` or `*_bytes`.
- [2026-09-05] Release signing is configured from `keystore.properties` (gitignored) or the environment, and **`assembleRelease` still succeeds without it**, producing `-unsigned.apk` → CI and a developer without the key must be able to prove the release variant compiles. v1+v2+v3 signing: v1 alone is rejected from API 30, and v2+ is what makes a side-load fast enough not to look broken during N40's fifteen minutes. `docs/APK-SIGNING.md` carries the backup procedure, including the step people skip — verifying the restore on a different machine before the first release.
- [2026-09-05] The debug LAN host is a **build-time substitution**, not a tracked address → `src/debug/network_security_config.template.xml` holds `__DEV_HOST__` and a Gradle task writes the substituted copy into `build/`. A developer sets `bonvicall.devHost` in `local.properties`; nobody's home IP reaches the repository and nobody edits a tracked file to test on their own network. With no host configured the placeholder LINE IS DROPPED rather than emitted — a placeholder shipped as a domain name, or a duplicate of `10.0.2.2`, is a config nobody has read, and lint rejects the duplicate as fatal.
- [2026-09-05] `docs/ON-DEVICE-TESTING.md` written → everything so far is verified in a JVM and not one call has been captured on hardware. The doc is the shortest path from a laptop to a captured call, including the two tests that matter more than the happy path: a call on the OTHER SIM must produce nothing anywhere, and a call made in aeroplane mode must arrive late rather than never.
- [2026-09-05] **No state discards the queue** (`DeviceAuthState.holdsQueue` is `true` for all four, asserted by a test) → the natural implementation of "the token expired" clears local state, and what that clears is calls that happened and never reached the server. N25 exists because that failure is silent: the phone looks healthy and the panel simply has fewer calls than the handset made.
- [2026-09-05] Only a refused **refresh** produces `auth_expired`; a single 401 on a normal request does not → a 401 is a stale access token, which `TokenAuthenticator` refreshes and replays once. A network failure during refresh is explicitly NOT expiry: the session may be perfectly valid and the phone simply in a lift, and holding the queue for a reason that was not true is its own fault.
- [2026-09-05] `UPDATE_REQUIRED` keeps sending (T83/N34) → the server accepts a stale client's queued records and refuses it only once the backlog is empty. A client that stopped sending would strand exactly the data the gate exists to protect. The state changes what the UI says, never what the queue does.
- [2026-09-05] `TokenAuthenticator` uses `runBlocking` → the second named exception to CONVENTIONS-CLIENT.md §8, for the same reason as `SessionStore`'s snapshot: OkHttp's `Authenticator` contract is synchronous, it runs on a background OkHttp thread and never on the main thread, and the alternative is no token refresh at all.
- [2026-09-05] Click-to-call discards commands older than **two minutes** and acknowledges them as stale (T80, UC-16) → the transport is not the risk (the hub measures 13 ms against a 5 000 ms bar); doze on MIUI/EMUI is. A number dialled ten minutes late is worse than one not dialled at all, because somebody answers it. Acknowledged rather than dropped, so the panel can tell "arrived late" from "never arrived" — different faults, different fixes.

### Phase 6 wiring (2026-09-05)

- [2026-09-05] `Int64 = Annotated[int, Field(json_schema_extra={"format": "int64"})]` lives in `src/core/wire.py` → Pydantic's `int` reaches OpenAPI as a formatless integer and openapi-generator maps that to a 32-bit `kotlin.Int`; `System.currentTimeMillis()` is ~1.77e12 against `Int.MAX_VALUE` of 2.1e9. Applied to 31 fields across all three surfaces — the nine Android reported, plus `CallbackEventIn.receiver_epoch_ms` (the same bug on the service surface, which nobody generating an Android client could see), the panel's byte counters, and the export cursor `seq`/`next_since`. Deliberately **not** applied to the audio upload counters: one recording is ~16 MB and a field that cannot overflow should not claim it can.
- [2026-09-05] `tests/test_wire_int64.py` fails in **both** directions → a field needing 64 bits that does not say so is the original bug; a field claiming 64 bits it cannot use makes the exception list unauditable, which is how the original bug survives review. Both exception lists carry a reason per entry, and a third test fails when an entry goes stale.
- [2026-09-05] The device realtime socket is registered; `test_every_router_module_is_registered` now walks `src/api/*/` and fails on a router nothing includes → `api/device/ws.py` sat finished and unreachable while its own tests passed, because they mounted it themselves. The RBAC harness can only see registered routes, so it was blind to exactly this.

### Incident: the development database was emptied (2026-09-05)

- [2026-09-05] **I ran `docker compose run -e POSTGRES_DB=bonvicall_headcheck backend alembic downgrade base` believing it targeted a throwaway. It did not.** `DATABASE_URL` is one whole string in `.env`; `POSTGRES_DB` never feeds it, so the downgrade dropped all 31 tables of the *development* database. Everything lost was script-generated (`make seed` + `make demo`) and was restored within minutes — no irreplaceable data existed. It was still a destructive action I should have confirmed before running.
- [2026-09-05] Fix, so the mistake cannot repeat: `migrations/env.py` **refuses a downgrade against the configured `database_url`** unless `-x db_url=...` (the only real way to choose a database here) or `-x i_know_this_is_destructive=yes` is given. Verified that it blocks the exact command that caused the damage, and that `upgrade`, the test suite's own migration, and an explicit `-x db_url` all still pass through.
- [2026-09-05] `make migrate-head-check` (T102) creates a scratch database, upgrades to head, checks it, downgrades and drops it → verifying the head should never involve typing `downgrade` anywhere near the development database.
- [2026-09-05] `AlertResponse.title_uz` / `body_uz` are **derived from `kind` at read time**, not served from the stored columns → the columns are written at raise time and are the record of what the alert said then; serving them meant every alert open on the running server still read the English "Device offline" after the Uzbek had been written. `kind` is the closed enum; the sentence is presentation. `core/messages_uz.py` stays the one place it lives, and the panel can drop its own `Record<AlertKind, MessageKey>` map.
- [2026-09-05] The server's alert wording is **the panel's wording, verbatim** → the panel had already written all 27 titles and hints because it had to route around the English fallback. Copying it programmatically from `panel/src/shared/i18n/uz.json` rather than retyping means the two cannot disagree on day one.
- [2026-09-05] The demo reports `oem_recorder: not_applicable` (not `denied`) for ROMs with no call recorder → `denied` is a transition into a broken state and raises `recording_route_lost` — "the recording method stopped working" — on a phone that never had that method and captures fine via `app_voice_recognition`. `not_applicable` is the state for a feature the OEM does not have, the same reason `contacts` uses it. `recording_route_lost` now fires once, for the handset that genuinely lost it.
- [2026-09-05] The demo posts capabilities **twice** for the two broken handsets → the drift alert is raised on the *transition*, so a phone that was never healthy raises nothing. Baseline-then-drift gives the panel a real "it worked at enrolment and then the OEM took it away" history, which is the only thing the capability time series exists for.
