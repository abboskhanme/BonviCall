/**
 * Alerts — stub. The route, its `<Gate>` and its nav entry are settled in
 * T21; T92 replaces this body and touches nothing above it.
 */
import { StubPage } from '@/shared/ui/StubPage'

export function AlertsPage() {
  return <StubPage titleKey="page.alerts" task="T92" />
}
