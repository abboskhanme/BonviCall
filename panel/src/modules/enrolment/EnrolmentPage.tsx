/**
 * Enrolment — stub. The route, its `<Gate>` and its nav entry are settled in
 * T21; T86 replaces this body and touches nothing above it.
 */
import { StubPage } from '@/shared/ui/StubPage'

export function EnrolmentPage() {
  return <StubPage titleKey="page.enrolment" task="T86" />
}
