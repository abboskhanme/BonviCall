/**
 * Numbers — stub. The route, its `<Gate>` and its nav entry are settled in
 * T21; T88 replaces this body and touches nothing above it.
 */
import { StubPage } from '@/shared/ui/StubPage'

export function NumbersPage() {
  return <StubPage titleKey="page.numbers" task="T88" />
}
