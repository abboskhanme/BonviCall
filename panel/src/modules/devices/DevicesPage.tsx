/**
 * Devices — stub. The route, its `<Gate>` and its nav entry are settled in
 * T21; T87 replaces this body and touches nothing above it.
 */
import { StubPage } from '@/shared/ui/StubPage'

export function DevicesPage() {
  return <StubPage titleKey="page.devices" task="T87" />
}
