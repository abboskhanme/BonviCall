/**
 * Dashboard — stub. The route, its `<Gate>` and its nav entry are settled in
 * T21; T85 replaces this body and touches nothing above it.
 */
import { StubPage } from '@/shared/ui/StubPage'

export function DashboardPage() {
  return <StubPage titleKey="page.dashboard" task="T85" />
}
