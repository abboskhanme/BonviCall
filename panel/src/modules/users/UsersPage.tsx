/**
 * Users — stub. The route, its `<Gate>` and its nav entry are settled in
 * T21; T149 replaces this body and touches nothing above it.
 */
import { StubPage } from '@/shared/ui/StubPage'

export function UsersPage() {
  return <StubPage titleKey="page.users" task="T149" />
}
