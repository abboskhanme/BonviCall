/**
 * AppVersions — stub. The route, its `<Gate>` and its nav entry are settled in
 * T21; T93 / T81 replaces this body and touches nothing above it.
 */
import { StubPage } from '@/shared/ui/StubPage'

export function AppVersionsPage() {
  return <StubPage titleKey="page.appVersions" task="T93 / T81" />
}
