/**
 * Settings — stub. The route, its `<Gate>` and its nav entry are settled in
 * T21; T93 replaces this body and touches nothing above it.
 */
import { StubPage } from '@/shared/ui/StubPage'

export function SettingsPage() {
  return <StubPage titleKey="page.settings" task="T93" />
}
