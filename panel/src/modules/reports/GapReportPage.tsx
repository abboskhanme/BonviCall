/**
 * GapReport — stub. The route, its `<Gate>` and its nav entry are settled in
 * T21; T91 replaces this body and touches nothing above it.
 */
import { StubPage } from '@/shared/ui/StubPage'

export function GapReportPage() {
  return <StubPage titleKey="page.gapReport" task="T91" />
}
