/**
 * StorageReport — stub. The route, its `<Gate>` and its nav entry are settled in
 * T21; T99 replaces this body and touches nothing above it.
 */
import { StubPage } from '@/shared/ui/StubPage'

export function StorageReportPage() {
  return <StubPage titleKey="page.storageReport" task="T99" />
}
