/**
 * Audit — stub. The route, its `<Gate>` and its nav entry are settled in
 * T21; T94 replaces this body and touches nothing above it.
 */
import { StubPage } from '@/shared/ui/StubPage'

export function AuditPage() {
  return <StubPage titleKey="page.audit" task="T94" />
}
