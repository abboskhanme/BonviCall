package uz.bonvi.call.ui.home

import androidx.compose.runtime.Composable
import uz.bonvi.call.R
import uz.bonvi.call.ui.StubScreen

/**
 * The employee's own view: is capture working, is anything queued. N41 —
 * transparency is a screen they can open, not a promise in a document.
 *
 * Stub. The destination and its route are settled in T22; T65 replaces this
 * body and does not touch the navigation graph.
 */
@Composable
fun HomeScreen() {
    StubScreen(titleRes = R.string.home_title, task = "T65")
}
