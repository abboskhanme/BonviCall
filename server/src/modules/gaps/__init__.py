"""Gaps — the audio capture-rate report (UC-23, N3, N4).

The product's own smoke alarm: it is how "a phone stopped producing audio"
becomes visible. Owns **no table** and holds the §2.1 read exception.
"""
