fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
# 2.13.5, up from 2.10.4, and bumped on its own rather than alongside the
# feature that forces it: the call-analysis SDK `google-genai` requires
# pydantic>=2.12.5 (docs/SPEC-ANALYTICS.md §4.3). Pydantic generates this API's
# OpenAPI document, so the proof that the bump is a no-op is `make contract`
# leaving contract/ byte-identical — checked when this pin moved, and the thing
# to check again the next time it does.
pydantic==2.13.5
# Backs pydantic's EmailStr: the login form validates the address at the edge
# rather than letting a typo reach a CITEXT unique index as a 500.
email-validator==2.2.0
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
argon2-cffi==23.1.0
python-multipart==0.0.20
httpx==0.28.1
structlog==24.4.0
# The scheduler process (worker.py). 3.x rather than 4.x: 4 is still beta and
# this is the process that deletes audio irreversibly.
apscheduler==3.10.4

pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.6
PyYAML==6.0.2
