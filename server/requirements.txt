fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
# 2.13.5, up from 2.10.4, bumped alone and ahead of the feature that forces it:
# the call-analysis SDK `google-genai` requires pydantic>=2.12.5
# (docs/SPEC-ANALYTICS.md §4.3).
#
# Pydantic generates this API's OpenAPI document, and this bump CHANGES it.
# 2.11+ emits `additionalProperties: true` on bare-dict fields and 2.12+ adds a
# `pattern` to Decimal-as-string fields: seven added lines in
# `contract/openapi-panel-v1.json`, none in the device document the phones'
# DTOs are generated from. Annotations only — no field, type or name moves — so
# `make contract` must be re-run and its diff committed alongside this pin.
# Pinning 2.12.5 instead buys nothing: it produces the same seven lines, so no
# version both satisfies the SDK and leaves the contract untouched.
pydantic==2.13.5
# Backs pydantic's EmailStr: the login form validates the address at the edge
# rather than letting a typo reach a CITEXT unique index as a 500.
email-validator==2.2.0
# Deliberately NOT moved with pydantic above: it declares `pydantic>=2.7.0`,
# resolves against 2.13.5, and the suite plus a real start-up (`/readyz`,
# `alembic upgrade head`) exercise the settings layer on it. An unforced bump
# is a second variable in a change whose whole point is being revertible alone.
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
argon2-cffi==23.1.0
python-multipart==0.0.20
httpx==0.28.1
structlog==24.4.0
# The scheduler process (worker.py). 3.x rather than 4.x: 4 is still beta and
# this is the process that deletes audio irreversibly.
apscheduler==3.10.4

# The SAP `.xlsx` reader (modules/sales). Newest stable at pin time.
#
# NOT a free choice between spreadsheet libraries: the sales export
# arrives in two generations whose money cells are told apart ONLY by
# `cell.number_format` (an old-export `#,##0` integer is a thousand times
# too large — 146,000 $ stored as 146 $). openpyxl is the only reader that
# exposes the format; python-calamine and xlsx2csv discard it, and the
# defect would be silent. `pandas` would bring a numeric stack this server
# has no other use for.
openpyxl==3.1.5

# AI providers — the official vendor SDK, imported lazily INSIDE the client
# (analysis/providers/gemini.py) so that a missing package is a clear error at
# call time rather than a server that will not start.
#
# ONE dependency, because phase 1 ships one provider: Gemini does both ASR and
# LLM (docs/SPEC-ANALYTICS.md §4.1). `anthropic` and `openai` are deliberately
# absent — each arrives with its own pinned SDK on the day its registry row
# does. `providers/openai_compat.py` is here without a pin on purpose: it is
# the shared protocol adapter, nothing in phase 1 has a registry row that uses
# it, and its import sits inside the call.
#
# This requires pydantic>=2.12.5, which is why the bump above went first and
# alone. Newest stable at pin time, resolved in the container rather than
# copied from BonviZvonki (§4.3).
google-genai==2.24.0

pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.6
PyYAML==6.0.2
